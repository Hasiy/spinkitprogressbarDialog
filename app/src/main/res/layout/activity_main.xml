<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="top.hasiy.spinkitProgressBarDemo.MainActivity">

    <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="120dp"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintBottom_toBottomOf="@+id/dialog1"
            android:gravity="center_vertical" app:layout_constraintHorizontal_bias="0.0">

        <ImageButton
                android:layout_width="70dp"
                android:layout_height="70dp"
                android:layout_centerHorizontal="true"
                android:background="@drawable/toast"
                android:contentDescription="@null"/>


    </RelativeLayout>

    <Button
            android:text="RotatingPlane效果进度条Dialog"
            android:layout_width="160dp"
            android:layout_height="40dp"
            android:id="@+id/dialog1"
            android:background="@color/colorPrimary"
            android:textColor="@color/defaultTextColor"
            tools:ignore="HardcodedText"
            android:layout_marginTop="168dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginStart="124dp"/>

    <Button
            android:text="DoubleBounce效果进度条Dialog"
            android:layout_width="160dp"
            android:layout_height="40dp"
            android:id="@+id/dialog2"
            android:background="@color/colorPrimary"
            android:textColor="@color/defaultTextColor"
            tools:ignore="HardcodedText"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginStart="124dp"
            android:layout_marginTop="56dp"
            app:layout_constraintTop_toBottomOf="@+id/dialog1"/>




</androidx.constraintlayout.widget.ConstraintLayout>